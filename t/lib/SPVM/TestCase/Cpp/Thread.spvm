class TestCase::Cpp::Thread {
  use Cpp::Thread;
  use Sys::Process;
  
  static method test : int () {
    
    my $foo = 4;
    my $thread = Cpp::Thread->new([$foo : int] method : void () {
      warn "AAA $foo";
    });
    
    $thread->join;

    return 1;
  }
}
